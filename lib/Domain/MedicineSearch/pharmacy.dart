class Pharmacy {
  String address;
  String name;
  int id;
  Pharmacy(this.address, this.name, this.id);
}

class Todo {
  int userId;
  int id;
  String title;
  Todo(this.userId, this.id, this.title);
}
